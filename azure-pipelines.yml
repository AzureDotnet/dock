pool:
  name: 'newpool'    # Specifies the name of the agent pool
  vmImage: 'windows-latest'  # Specifies the OS (in this case, Windows)

jobs:
- job: Build
  pool:
    name: 'newpool'    # Specifies the custom agent pool
    vmImage: 'windows-latest'  # Runs on the latest Windows image
  steps:
  - script: echo Hello, World!
    displayName: 'Print message'